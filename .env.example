# URL base de la API. Si tu backend corre en otro puerto en desarrollo,
# defínela aquí (Vite la expone como import.meta.env.VITE_API_URL).
# Sin definir, el cliente axios (src/lib/api.ts) usa "/api" por defecto.
VITE_API_URL=http://localhost:3000/api
