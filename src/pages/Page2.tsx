import { Typography } from 'antd'

const { Paragraph } = Typography

export function Page2() {
  return (
    <Paragraph>
      Otra página de ejemplo. Duplica esta estructura para nuevas secciones y
      regístralas en <code>src/config/menu.config.tsx</code>.
    </Paragraph>
  )
}
