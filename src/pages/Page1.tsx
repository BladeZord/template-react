import { Typography } from 'antd'

const { Paragraph } = Typography

export function Page1() {
  return (
    <Paragraph>
      Contenido de ejemplo. Añade aquí tus vistas y lógica. Las rutas y el menú
      se configuran en <code>src/config/menu.config.tsx</code>.
    </Paragraph>
  )
}
